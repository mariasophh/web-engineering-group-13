import ReactDOM from 'react-dom';
import { Router } from './Routes';

import './css/main.css';

// Base for rendering the components on the div with id 'root'
ReactDOM.render(Router(), document.getElementById('root'));
